name: Build Docker EDA Image
on:
  push:
    paths:
      - "kubeflow.Dockerfile"
      - "Pipfile"
      - "Pipfile.lock"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Build Docker Image with DockerHub
        run: |
          cd $GITHUB_WORKSPACE
          BASE_NAME="$BASE_NAME-kubeflow"
          echo ${PASSWORD} | docker login -u $USERNAME --password-stdin
          docker pull $BASE_NAME || true
          docker build --cache-from $BASE_NAME -t $BASE_NAME -f kubeflow.Dockerfile .
        env:
          USERNAME: ${{ secrets.DOCKER_USERNAME }}
          PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
          BASE_NAME: ${{ github.repository }}
